# Тест №1

1. Что является основной целью метода Макларена-Марсальи генерации псевдослучайных чисел?
   а) увеличение периода последовательности за счет комбинации двух генераторов
   б) сокращение времени вычисления за счет упрощения формул
   в) улучшение случайности путем перемешивания вывода одного генератора с помощью другого
   г) генерация только целочисленных последовательностей без нормировки (или нумеровки я не пон)

2. Сколько независимых генераторов псевдослучайных чисел используется в методе Макларена-Марсальи?
   а) 1
   б) 2
   в) 3
   г) 4 и более

3. Какова роль вспомогательной таблицы (буфера) в методе Макларена-Марсальи?
   а) хранение начальных значений обоих генераторов
   б) временное хранение последовательностей от первого генератора для последующего выбора вторым
   в) вычисление модуля для нормировки чисел
   г) проверка статистических тестов на случайность

4. Как второй генератор взаимодействует в таблице?
   а) заполняет таблицу новыми значениями
   б) генерирует индексы для выбора и замены элементов в таблице
   в) проверяет таблицу на дубликаты
   г) нормирует значения таблицы на 0,...(что-то написала на доске, какое-то значение)

5. Какой шаг выполняется после выбора элементов в этом методе?
   а) элемент удаляется из таблицы навсегда
   б) выбранный элемент заменяется новым значением от первого генератора
   в) таблица полностью перезаполняется
   г) индекс добавляется к значению элемента

6. Какое преимущество метода Макларена-Марсальи по сравнению с простыми линейными конгруэнтными генераторами?
   а) он гарантирует бесконечный период последовательности
   б) уменьшает корреляцию между последовательными числами за счет перемешивания
   в) не требует модульной арифметики
   г) работает с маленькими модулями (n < 10)

7. Какой параметр K обозначает в описании метода Макларена-Марсальи?
   а) модуль конгруэнтного генератора
   б) начальное значение для генераторов
   в) размер вспомогательной таблицы
   г) количество итераций для метода случайностей

8. Какой недостаток может иметь метод ММ при малом К?
   а) снижение качества случайностей из-за недостаточного перемешивания
   б) увеличение скорости генерации
   в) потеря периодичности
   г) невозможность нормировки на каком-то значении

9. В методе ММ первый генератор обычно отвечает за...
   а) генерацию индексов для таблицы
   б) заполнение таблицы псевдослучайными числами
   в) проверку статистических критериев
   г) выбор уровня значимости для тестов

10. Для чего рекомендуется проверять точность генерированной последовательности в методе ММ с помощью критерия Колмогорова и Пирсона?
    а) чтобы подтвердить равномерность распределения и отсутствия корреляций
    б) чтобы рассчитать размер таблицы
    в) чтобы выбрать параметры для генераторов
    г) чтобы сгенерировать новое начальное значение
